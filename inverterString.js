let str = "Seria um prazer fazer parte dessa equipe!"; // a string a ser invertida
let invertedStr = ""; // a string invertida

for (let i = str.length - 1; i >= 0; i--) {
  invertedStr += str[i];
}

console.log(str); // imprime "olpmexe"
console.log(invertedStr); // imprime "olpmexe"